# Copy this file to .env and fill in real values.
# .env is gitignored - secrets never get committed.

# Needed for the tagging stage (smoke_test.py --live, tag_batch.py,
# parse_validate.py retries). The account must have API credits;
# the full batch run costs roughly $15-31.
ANTHROPIC_API_KEY=
